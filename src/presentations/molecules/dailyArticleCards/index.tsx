import * as React from "react";
import { Outer, Left, Right } from "./element";

export const DailyArticleCard = () => {
  return (
    <Outer>
      <Left>a</Left>
      <Right>b</Right>
    </Outer>
  );
};
